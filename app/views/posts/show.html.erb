<p id="notice"><%= notice %></p>

<div>
  <% @post.photos.each do |photo| %>
    <%= link_to attachment_image_tag(photo, :file, :fill, 100, 100), attachment_url(photo, :file) %>
  <% end %>
</div>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
